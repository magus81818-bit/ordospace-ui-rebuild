DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/ordospace"
JWT_SECRET="교체할-길고-무작위한-비밀문자열"
PORT=3000
# 로컬 PostgreSQL은 false, Render 등 외부 DB는 true
DATABASE_SSL=false
# 프론트 오리진(배포 후 Vercel 주소). 비우면 개발용 전체 허용
CORS_ORIGIN=

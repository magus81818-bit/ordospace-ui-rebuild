# domain

ORDOSPACE의 핵심 용어와 상태를 담당합니다.

## 향후 후보

- `Role`: `client`, `admin`, `worker`
- `ProjectStatus`
- `ApprovalStatus`
- `TimelineEvent`
- `WorkCard`

비유하면 용어 사전입니다. 같은 말을 여러 파일에서 다르게 쓰지 않도록 기준을 잡는 곳입니다.

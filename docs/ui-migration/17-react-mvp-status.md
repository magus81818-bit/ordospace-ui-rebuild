# React MVP Status

`apps/web` is an internal dashboard experiment and regression reference. It is not the existing ORDOSPACE public product, does not own `/`, and is excluded from `dist/product-static` and Vercel output. Its independent build remains available through `npm run build:react-mvp`.

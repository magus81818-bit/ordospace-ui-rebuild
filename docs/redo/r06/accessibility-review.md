# Accessibility Review

다섯 Admin 화면에서 table header scope, table overflow accessible name/keyboard focus, tab role/selected/controls, filter/input accessible name, dialog/sheet name·modal semantics, validation error association, visible focus, disabled semantics, reduced-motion을 검증했습니다. 프로젝트 표 행은 Enter/Space로 기존 click action을 실행하며 overlay는 Escape와 focus return을 지원합니다.


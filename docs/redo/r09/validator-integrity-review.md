# Validator integrity review

Round 3/4 Worker stylesheet isolation and Round 6 Worker descendant recognition retain their API/backend/Shell prohibitions. Round 9 rejects missing artifacts, failed artifact summaries, incorrect Matrix values, incomplete 73-row coverage, browser errors, overflow, and product changes.

The final integrity artifact additionally requires zero:

- hardcoded `pass: true` evidence rows
- invalid non-assertions
- generic browser assertions
- missing browser assertions
- accepted directory-only evidence
- expectation relaxations

# Browser health review

Console errors, page errors, request failures, HTTP failures, unhandled rejections, observer loops, duplicate actions, and horizontal overflow are separately counted. QA-isolated API behavior is excluded from operating health.

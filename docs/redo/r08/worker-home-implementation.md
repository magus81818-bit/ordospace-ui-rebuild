# Worker Home implementation

The preserved section order is page header, four KPI cards, revision queue, in-progress work, and pending work.

- UI-033 keeps the four KPI labels, order, values, calculations, and deep links. SalesOps metric hierarchy, compact padding, dark surface, border, hover, zero, and warning treatments are applied.
- UI-034 keeps the existing revision filter, queue order, PM comment, deadline, and card navigation. Critical/revision tone and an explicit empty surface are applied.
- UI-035 keeps assigned in-progress cards and adds a visual QC progress treatment calculated from the same checklist values.
- UI-036 keeps pending-card order, due date, click behavior, and empty state while adopting the dense card-row grammar.

Screenshots cover 1440×1000, 1280×900, 1024×1366, 768×1024, 390×844, and 360×800, plus zero, warning, revision, empty, overdue/long-content states.


# Frozen public regression

The six frozen public routes are landing, auth, terms, privacy, support, and workspace selection. Each route is checked at desktop, tablet, and mobile for 18 total cases.

The audit compares DOM signatures, representative computed styles and boxes, authenticated-scope leakage, and horizontal overflow. All 18 cases pass. No public product file or selector was changed.


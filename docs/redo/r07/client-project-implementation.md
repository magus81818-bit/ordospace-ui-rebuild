# Client Project Implementation

UI-025 keeps the existing header position, project metadata, progress, CTA, and Korean copy. UI-026 retains button-group semantics, filter values/results, pressed state, selected tab controls, and no-result behavior.

UI-027 derives timeline surface grammar from the SalesOps chart container while preserving ModuleCard grouping, step order, Gate meaning, lifecycle, and card clicks. Evidence covers pass/fail and long mobile content without changing operating fixtures.

UI-028 keeps file names, dates, links, ordering, and empty behavior. The correction adds populated, page-local empty, and long-filename mobile evidence.

UI-029 uses code-only Dialog evidence. Real browser assertions now cover accessible name, modal state, focus entry, both focus-loop edges, background focus blocking, Escape close, `aria-hidden` reset, scroll restoration, trigger focus return, backdrop close, duplicate-close safety, and 390x844 bounds/overflow. A page-local alert supplies error visual evidence without invoking an API or persisted transition.

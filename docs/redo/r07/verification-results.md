# Verification Results

Results are generated by `tests/redo/r07` and recorded in `artifacts/redo/r07/verification-summary.json`, `test-results.json`, and the browser artifacts. A result is reported as complete only if the full current and prior-round matrix passes.

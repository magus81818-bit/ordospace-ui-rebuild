# CSS Isolation

Every selector is below `body.auth-on` and one of the three Client screen IDs. The audit rejects Admin, Worker, public and Shell selectors, raw colors, unresolved tokens, remote fonts, and new `!important` declarations.

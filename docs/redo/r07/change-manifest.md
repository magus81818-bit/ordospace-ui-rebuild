# Change Manifest

Product correction: `app/ui/components/client.ui.js` adds focus containment/background redirection and mutation-driven focus return to the existing modal semantic decorator. No business handler is replaced.

Test correction: Round 7 browser tests now generate matrix-authoritative inventory/catalog, explicit state schema, 15 additional state screenshots, measured accessibility/interactions, destructive-action-free lifecycle spies, and QA fixture isolation. The validator now reads artifacts independently, validates schemas/files/matrix fields, scans invalid assertions, and writes only the verification summary.

Artifacts updated: inventory, catalog, state coverage, interaction, accessibility, QA isolation, layout, non-Client, public, test results, verification summary. Docs expanded and correction report added.

No Client data/copy/route/lifecycle meaning/localStorage key/API/backend, Admin/Worker/Shell/public, main, deployment, or Vercel alias change.

# Mobile shell implementation

The existing mobile header, drawer, and bottom tabs are retained below 1024px. The drawer is a token-derived overlay because SalesOps has no exact ORDOSPACE mobile analogue. It exposes dialog semantics, deterministic open/close state, Escape handling, and focus return.

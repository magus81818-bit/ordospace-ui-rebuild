# CSS isolation

All Round 5 selectors are rooted at `body.auth-on`. The shell stylesheet has zero public/auth-off selectors, zero raw color literals, zero unresolved token references, and no change to landing/public component styles.

# Public regression

Landing and public routes remain frozen. The new stylesheet is linked globally but has only `body.auth-on` rules, so it produces no token or component style on auth-off surfaces. Public screen containers and routes were not edited.

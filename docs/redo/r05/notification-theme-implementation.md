# Notification and theme implementation

Notification content, targets, categories, and unread data remain unchanged. The panel now exposes expanded/hidden state and Escape focus return. Theme choices remain `light`, `dark`, and `system`, persist under `ordo_theme`, and publish `aria-pressed` without changing resolution logic.
